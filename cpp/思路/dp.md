# 背包问题

## 0-1 背包
注意 0-1 背包在遍历的时候需要逆序遍历  
```cpp
for (int i = 0; i < nums.size(); ++i){
    for (int j = plus; j >= nums[i]; --j){
        dp[j] += dp[j - nums[i]];
    }
}
```
如果正向遍历的话，比如物品的价值为 n，那么 dp\[n\] 的时候和 dp[n + n]以及后续相当于重复拿了这个物品  
那么这个时候应该是完全背包的写法
