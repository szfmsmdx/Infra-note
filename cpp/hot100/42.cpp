#include<vector>
#include<stack>
using namespace std;

class Solution {
public:
    int trap(vector<int>& height) {
        // 单调栈
        int res = 0;
        stack<int> st; st.push(0);
        for (int i = 1; i < height.size(); ++i){
            
        }
        return res;
    }
};